/**
 * @memberof module:Constants
 * @namespace SEND_EVENTS_URL
 * @description The URL to send events to the server
 * @default '/add/events/v1.json'
 */
export const SEND_EVENTS_URL = '/add/events/v1.json';
/**
 * @memberof module:Constants
 * @namespace SEND_PUSH_NOTIFICATION_URL
 * @description The URL to send push notification to the server
 * @default '/native/subscription/push'
 */
export const SEND_PUSH_NOTIFICATION_URL = '/native/subscription/push';
/**
 * @memberof module:Constants
 * @namespace SEND_PUSH_NOTIFICATION_INTERACTION_URL
 * @description The URL to send push notification interaction to the server
 * @default '/native/interaction/notification'
 */
export const SEND_PUSH_NOTIFICATION_INTERACTION_URL =
  '/native/interaction/notification';

/**
 * @memberof module:Constants
 * @namespace GET_APP_INITIAL_CREDENTIALS_URL
 * @description The URL to get the app initial credentials - this is used to get the user id and the session id
 * @default '/get/key/v1.json'
 */
export const GET_APP_INITIAL_CREDENTIALS_URL = '/get/key/v1.json';
